// Disabled for Carbide India — no demo data. Real employees are invited via
// /admin/employees once the first admin is bootstrapped.

console.log("Demo seed disabled — Carbide India starts with empty data.");
process.exit(0);
